---
phase: 02-pwa-fullscreen-face-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/tablet/public/manifest.json
  - apps/tablet/src/lib/fullscreen.ts
  - apps/tablet/src/lib/fullscreen.test.ts
autonomous: false
requirements: [R3, R4]

must_haves:
  truths:
    - "App in PWA standalone mode runs without browser chrome (no address bar, no navigation)"
    - "requestFullscreen() is not called when app is running in standalone mode"
    - "requestFullscreen() is still called when app is running in regular browser mode"
    - "manifest.json uses display: standalone (not fullscreen, which iOS ignores)"
    - "Face detection wakes installation within 3 seconds of face appearing"
    - "Face detection sleeps installation within 30 seconds of face disappearing"
  artifacts:
    - path: "apps/tablet/public/manifest.json"
      provides: "PWA manifest with correct standalone display mode"
      contains: '"display": "standalone"'
    - path: "apps/tablet/src/lib/fullscreen.ts"
      provides: "isStandaloneMode() detection + conditional requestFullscreen()"
      exports: ["isStandaloneMode", "requestFullscreen"]
    - path: "apps/tablet/src/lib/fullscreen.test.ts"
      provides: "Unit tests for standalone detection and conditional fullscreen"
      min_lines: 40
  key_links:
    - from: "apps/tablet/src/components/screens/SleepScreen.tsx"
      to: "apps/tablet/src/lib/fullscreen.ts"
      via: "import { requestFullscreen }"
      pattern: "requestFullscreen"
    - from: "apps/tablet/src/lib/fullscreen.ts"
      to: "navigator.standalone / matchMedia"
      via: "isStandaloneMode() runtime check"
      pattern: "isStandaloneMode"
---

<objective>
Fix PWA standalone mode and fullscreen behavior so the tablet app runs as an autonomous kiosk on iPad. Verify face detection wake/sleep cycle works on target device.

Purpose: The current manifest.json uses `display: "fullscreen"` which iOS silently ignores (falling back to standalone). The Fullscreen API is called unconditionally, which can throw errors in standalone mode. These fixes ensure the app behaves correctly when added to the iPad Home Screen and run in Guided Access kiosk mode.

Output: Corrected manifest.json, standalone-aware fullscreen.ts with unit tests, verified kiosk behavior on iPad.
</objective>

<execution_context>
@/Users/janos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/janos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pwa-fullscreen-face-detection/02-RESEARCH.md

@apps/tablet/src/lib/fullscreen.ts
@apps/tablet/public/manifest.json
@apps/tablet/index.html
@apps/tablet/src/components/screens/SleepScreen.tsx

<interfaces>
<!-- Key types and contracts the executor needs. -->

From apps/tablet/src/lib/fullscreen.ts (CURRENT — to be modified):
```typescript
export function requestFullscreen(): void;
// NEW export to add:
// export function isStandaloneMode(): boolean;
```

From apps/tablet/src/components/screens/SleepScreen.tsx (consumer — no changes needed):
```typescript
import { requestFullscreen } from '../../lib/fullscreen';
// SleepScreen calls requestFullscreen() on tap/click — this will now
// automatically skip the Fullscreen API in standalone mode.
```

From packages/shared/src/constants.ts:
```typescript
export const FACE_DETECTION = {
  WAKE_THRESHOLD_MS: 3000,
  SLEEP_THRESHOLD_MS: 30000,
  DETECTION_INTERVAL_MS: 500,
  MIN_CONFIDENCE: 0.5,
} as const;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add standalone detection to fullscreen.ts with tests + fix manifest</name>
  <files>apps/tablet/src/lib/fullscreen.test.ts, apps/tablet/src/lib/fullscreen.ts, apps/tablet/public/manifest.json</files>
  <behavior>
    - isStandaloneMode() returns true when navigator.standalone is true (iOS Safari proprietary)
    - isStandaloneMode() returns true when matchMedia('(display-mode: standalone)') matches
    - isStandaloneMode() returns false when neither condition is met (regular browser)
    - requestFullscreen() does NOT call document.documentElement.requestFullscreen in standalone mode
    - requestFullscreen() DOES call document.documentElement.requestFullscreen in regular browser mode
    - manifest.json has display: "standalone" (not "fullscreen")
    - manifest.json does NOT have orientation field (iOS ignores it, creates false confidence)
  </behavior>
  <action>
    **Step 1 (RED): Create fullscreen.test.ts**

    Create `apps/tablet/src/lib/fullscreen.test.ts` with tests covering all 7 behaviors above. Mock `navigator.standalone`, `window.matchMedia`, and `document.documentElement.requestFullscreen` / `webkitRequestFullscreen` using vi.spyOn / Object.defineProperty.

    Test structure:
    ```
    describe('isStandaloneMode', () => {
      test: returns true when navigator.standalone is true
      test: returns true when matchMedia matches standalone display-mode
      test: returns false when neither condition is met
    })
    describe('requestFullscreen', () => {
      test: skips Fullscreen API call in standalone mode
      test: calls requestFullscreen() in browser mode
      test: calls webkitRequestFullscreen() when standard API unavailable
    })
    describe('manifest.json', () => {
      test: has display: standalone (read and parse the JSON file)
    })
    ```

    Run tests — they MUST fail (fullscreen.ts doesn't export isStandaloneMode yet, manifest still says "fullscreen").

    **Step 2 (GREEN): Implement changes**

    Update `apps/tablet/src/lib/fullscreen.ts`:
    - Add exported `isStandaloneMode()` function that checks `navigator.standalone` (iOS proprietary, cast navigator) first, then falls back to `window.matchMedia('(display-mode: standalone)').matches`
    - Modify `requestFullscreen()` to call `isStandaloneMode()` first and return early if true
    - Keep the existing webkit fallback for non-standalone browser mode

    Update `apps/tablet/public/manifest.json`:
    - Change `"display": "fullscreen"` to `"display": "standalone"`
    - Remove `"orientation": "portrait"` (iOS ignores it per research)

    Run tests — all MUST pass.

    **Step 3: Typecheck**
    Run `pnpm typecheck` to verify no type errors introduced.
  </action>
  <verify>
    <automated>cd /Users/janos/Desktop/VAKUNST/code/mein.ung/meinungeheuer && pnpm --filter @meinungeheuer/tablet exec vitest run src/lib/fullscreen.test.ts -x && pnpm typecheck</automated>
  </verify>
  <done>
    - isStandaloneMode() exported and detects PWA standalone mode via navigator.standalone and matchMedia
    - requestFullscreen() skips Fullscreen API in standalone mode, calls it in browser mode
    - manifest.json has display: "standalone", no orientation field
    - All unit tests pass, typecheck clean
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify kiosk behavior on target iPad</name>
  <files>N/A — manual verification only, no files modified</files>
  <action>
    Present the verification checklist to the user. This is a human-verify checkpoint — all code changes are already complete from Task 1. The user must test on the physical target iPad to confirm PWA standalone mode and face detection work correctly in the real kiosk environment.
  </action>
  <verify>User confirms "approved" after testing all checklist items on iPad.</verify>
  <done>
    - iPad runs app from Home Screen in true standalone mode (no browser chrome)
    - Face detection wakes within ~3s, sleeps within ~30s
    - Tap-to-start fallback works if camera unavailable
  </done>
  <what-built>
    PWA standalone mode detection and conditional fullscreen. The app now:
    1. Declares itself as a standalone PWA in manifest.json
    2. Skips the Fullscreen API when running as an installed PWA (Add to Home Screen)
    3. Still uses Fullscreen API when running in regular Safari browser
    4. Face detection (already implemented) uses camera for wake/sleep cycle
  </what-built>
  <how-to-verify>
    **Setup (one-time on target iPad):**
    1. Settings > Apps > Safari > Camera: set to "Allow" (grants blanket camera permission)
    2. Settings > Display and Brightness > Auto-Lock: set to "Never"
    3. Open Safari, navigate to the tablet app URL
    4. Tap Share button > "Add to Home Screen" > "Add"

    **Test PWA standalone mode:**
    5. Open the app from the Home Screen icon (NOT Safari)
    6. Verify: No Safari address bar, no navigation controls, app fills screen
    7. Verify: Status bar area shows with transparent/black-translucent style
    8. Open Safari developer tools (Mac > Safari > Develop > iPad) — check console for NO `requestFullscreen` errors

    **Test face detection wake/sleep:**
    9. With app on sleep screen (breathing dot), stand in front of iPad camera
    10. Verify: App wakes within ~3 seconds (transitions from sleep to welcome screen)
    11. Walk away from the iPad
    12. Verify: App sleeps within ~30 seconds (transitions back to sleep screen with breathing dot)

    **Test tap-to-start fallback:**
    13. If camera permission was not granted, verify tapping the breathing dot still triggers wake

    **Optional — Guided Access lockdown:**
    14. Triple-click side button > Guided Access > Start
    15. Verify: Home button/swipe-up disabled, app cannot be exited
    16. Triple-click again to exit Guided Access
  </how-to-verify>
  <resume-signal>Type "approved" if kiosk behavior works correctly, or describe any issues observed.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter @meinungeheuer/tablet exec vitest run src/lib/fullscreen.test.ts -x` — all standalone detection and conditional fullscreen tests pass
- `pnpm typecheck` — no type errors across workspace
- `pnpm test` — full test suite passes (no regressions)
- Manual: iPad runs app in standalone mode without browser chrome
- Manual: Face detection wake (3s) and sleep (30s) cycle works autonomously
</verification>

<success_criteria>
- manifest.json uses `display: "standalone"` (not "fullscreen")
- `isStandaloneMode()` correctly detects PWA standalone mode on iOS and other platforms
- `requestFullscreen()` is a no-op in standalone mode
- Face detection wake/sleep cycle runs autonomously on target iPad
- No browser chrome visible when app is launched from Home Screen
- All automated tests pass, typecheck clean
</success_criteria>

<output>
After completion, create `.planning/phases/02-pwa-fullscreen-face-detection/02-01-SUMMARY.md`
</output>
