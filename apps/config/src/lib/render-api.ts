/**
 * render-api.ts — Placeholder client for the print-renderer service.
 *
 * Replace the stub implementations with real fetch calls once the
 * print-renderer API endpoints are finalised.
 */

export async function renderPreview(
  _endpoint: string,
  _body: Record<string, unknown>
): Promise<void> {
  console.warn('[render-api] renderPreview: render-api not configured');
}
