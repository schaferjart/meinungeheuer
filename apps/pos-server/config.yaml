# POS Printer Configuration — minimal image printer
# All rendering (fonts, layout, dithering) happens in the cloud print-renderer.

printer:
  connection: "usb"
  vendor_id: 0x1fc9
  product_id: 0x2016
  network_host: "192.168.1.100"
  network_port: 9100
  paper_width: 48

server:
  host: "0.0.0.0"
  port: 9100
